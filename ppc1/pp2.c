// VariableSize - output the size of each type of
// variable
 #include <stdio.h>
 //#include "Fred.h"

/*using namespace std;*/
//Function prototype
/*
float calcAvg(float [], int);

int main(int nNumberofArgs, char* pszArgs[])
{
int i,n=5;
float *x, avg;
for(i=0;i<5;i++){scanf("%f",&x[i]);}

calcAvg(x,n);
printf("mean= %f",avg);
//system("PAUSE");
return 0;
}
float calcAvg(float score[], int SIZE)
{

int index; //Counter set to index array.
float total = 0; //Accumulator.
float avg = 0; //Variable to hold average score.

//Index through array to get test scores
for(index = 0; index < 5; index++)//Note 10 is the actual size of array


//Variable to hold total of all scores.
total += score[index];
avg = (total/(float)SIZE);
return avg;
}
*/
void ttt()
{
    printf("Hello Hi!!!");
}
